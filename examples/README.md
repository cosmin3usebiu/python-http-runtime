# Examples

Examples for R002 should use offline or caller-supplied transports.

R002 does not ship live `requests`, `httpx`, `aiohttp`, or urllib transports.
Examples must not imply live HTTP transport support unless a separate approved
transport work package exists.

See `examples/basic/README.md` for a minimal offline usage shape.

# API Documentation

The curated API reference is maintained in `docs/API-Reference.md`.

The package-root public API remains:

- `HttpRuntime`
- `HttpRequest`
- `HttpResponse`
- `RuntimeSettings`
- `Transport`

Submodule API candidates are documented separately and are not package-root
exports.
